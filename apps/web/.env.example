# Copy this to .env.production (or set it in your host's environment) once
# the frontend and the API are deployed on different origins. Leave unset
# for local dev — Vite's dev proxy (vite.config.ts) forwards /api to the
# local NestJS server instead.
VITE_API_URL=https://api.example.com/api
